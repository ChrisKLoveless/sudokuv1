export function numMatch(num1, num2) {
  if (num1 === num2) {
    return true;
  } else 
    return false;
}

export function addTen(num1, num2) {
  const add = num1 + num2;
  if(add === 10) {
    return true;
  } else
    return false;
}